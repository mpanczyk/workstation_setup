# Ansible for my comfortable working environment

## Instalation/Application

```bash
sudo apt-get update
sudo apt-get install ansible
sudo ansible-pull -U https://github.com/mpanczyk/workstation_setup.git
```
